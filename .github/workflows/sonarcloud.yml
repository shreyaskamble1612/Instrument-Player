name: SonarCloud Scan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      - name: Install dependencies
        run: npm install
      - name: Run tests (if any)
        run: npm test
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@3f1b2a1d2e4c5f6a7b8c9d0e1f2a3b4c5d6e7f8a
        with:
          organization: YOUR_SONARCLOUD_ORG
          token: ${{ secrets.SONAR_TOKEN }}
          projectKey: YOUR_PROJECT_KEY
          projectName: "Instrument-Player"
